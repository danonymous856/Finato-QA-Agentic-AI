You are an expert QA engineer and test analyst.

Given the following software requirements and acceptance criteria, generate a comprehensive set of high-quality, atomic test cases.

Each test case should:
- Have a unique ID in the form TC_XXX
- Have a clear, concise title
- Include a short list of imperative steps
- Describe a single, well-scoped scenario
- Include a clear expected result
- Include a priority: Low, Medium, High, or Critical

Return ONLY a JSON array of test case objects with the following schema:
{
  "test_case_id": "TC_001",
  "title": "Login with valid credentials",
  "steps": [
    "Open login page",
    "Enter username",
    "Enter password",
    "Click login"
  ],
  "expected_result": "User should be logged in",
  "priority": "High"
}

